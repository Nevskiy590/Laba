name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Build (ваш шаг сборки)
        run: echo "Здесь ваша сборка" > index.html

      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.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 }}
          publish_dir: ./Laba 

      - name: Debug
	  run: |
	  echo "Токен: ${{ secrets.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 }}"
	  git remote -v